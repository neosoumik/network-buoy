apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: network-buoy-logs
  namespace: network-buoy
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  # Remove storageClassName to use cluster default, or set to "gp2" for EKS
  # storageClassName: gp2
